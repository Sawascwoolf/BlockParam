<Window x:Class="BlockParam.UI.DiffPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BlockParam.UI"
        xmlns:loc="clr-namespace:BlockParam.Localization"
        Title="{Binding Title}"
                Width="750" Height="450"
        MinWidth="500" MinHeight="300"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="12">

    <DockPanel Margin="12">
        <!-- Buttons at bottom -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <local:ZoomIndicator Margin="0,0,12,0" VerticalAlignment="Center"/>
            <Button Content="{loc:Loc DiffPreview_ApplyChanges}" Command="{Binding ApplyCommand}"
                    Width="110" Height="28" Margin="0,0,8,0"/>
            <Button Content="{loc:Loc Dialog_Cancel}" Command="{Binding CancelCommand}"
                    Width="80" Height="28"/>
        </StackPanel>

        <!-- Summary -->
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Summary}"
                   FontSize="12" Foreground="#555" Margin="0,8,0,0"/>

        <!-- Diff table -->
        <DataGrid ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#CCCCCC" BorderThickness="1"
                  AlternatingRowBackground="#FAFAFA"
                  RowBackground="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{loc:Loc Column_Path}" Binding="{Binding MemberPath}" Width="2*"/>
                <DataGridTextColumn Header="{loc:Loc Column_Type}" Binding="{Binding Datatype}" Width="70"/>
                <DataGridTextColumn Header="{loc:Loc Column_Old}" Binding="{Binding OldValue}" Width="100"/>
                <DataGridTextColumn Header="{loc:Loc Column_New}" Binding="{Binding NewValue}" Width="100"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <!-- Unchanged rows: gray text -->
                        <DataTrigger Binding="{Binding IsChanged}" Value="False">
                            <Setter Property="Foreground" Value="#AAAAAA"/>
                        </DataTrigger>
                        <!-- Changed rows: green tint -->
                        <DataTrigger Binding="{Binding IsChanged}" Value="True">
                            <Setter Property="Background" Value="#E8F5E9"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </DockPanel>
</Window>
